import { Text, View } from "react-native";

export default function ProfileScreen() {
    return (
        <View className="p-6 bg-background flex flex-col items-center justify-center h-full">
            <Text className="text-4xl font-bold text-foreground">ProfileScreen</Text>
        </View>
    );
}